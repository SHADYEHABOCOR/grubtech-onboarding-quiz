services:
  - type: web
    name: grubtech-quiz
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    disk:
      name: quiz-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
